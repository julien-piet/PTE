Every time you restart WebArena environment, you need to set up a few things.

Gitlab PAT:
go to the website and get the PAT and pass it into .mpcenv as GITLAB_PAT_TOKEN



Before running the agent, make sure each of the servers are running.